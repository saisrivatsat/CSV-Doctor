{
  "name": "csv-doctor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Diagnose and repair delimiters, headers, dates, and duplicates entirely in the browser.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/app.js && node --check src/csv-doctor.js",
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
